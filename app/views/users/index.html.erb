<% if flash[:notice] %>
<p id="notice">
    <%= flash[:notice] %>
    </p>

<% end %>
<%= render 'books/list', books: @books %>


<h2>Users</h2>
<% @user.each do |user| %>
<div>
 <%= image_tag('no_image.jpg') %>
 
<p>name：<%= user.name %></p>
  <%= link_to "show", user_path(user) %>
</div>
<% end %>
<%= paginate @users %>